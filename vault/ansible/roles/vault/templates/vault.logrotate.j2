{{ vault_log_dir }}/*.log {
    daily
    missingok
    rotate 30
    compress
    notifempty
    create 640 {{ vault_user }} {{ vault_group }}
    postrotate
        systemctl reload vault > /dev/null 2>&1 || true
    endscript
}